<Window x:Class="WpfApp12._05.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp12._05"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" x:Name="MessageTextBox" Margin="10" VerticalScrollBarVisibility="Auto"
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="Write your message..."
                 Foreground="White" Background="#333"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
            <Button Content="Connect" Click="ConnectButton_Click" Margin="5"
                    Style="{StaticResource MaterialDesignRaisedButton}" Background="#2196F3" Foreground="White"/>
            <Button Content="Disconnect" Click="DisconnectButton_Click" Margin="5"
                    Style="{StaticResource MaterialDesignRaisedButton}" Background="#F44336" Foreground="White"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="10">
        </StackPanel>

        <TextBlock Grid.Row="3" TextWrapping="Wrap" Text="{Binding Message}" Margin="10"/>
    </Grid>
</Window>
